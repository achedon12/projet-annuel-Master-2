framework:
    messenger:
        failure_transport: failed

        transports:
            # https://symfony.com/doc/current/messenger.html#transport-configuration
            async:
                dsn: '%env(MESSENGER_TRANSPORT_DSN)%'
                options:
                    use_notify: true
                    check_delayed_interval: 60000
                retry_strategy:
                    max_retries: 3
                    multiplier: 2
            failed: 'doctrine://default?queue_name=failed'
            # sync: 'sync://'

        default_bus: messenger.bus.default

        buses:
            messenger.bus.default: []

        routing:
            # Les emails sont livrés en ligne (synchrone) et non via le transport
            # async : plus besoin de `messenger:consume async` pour que les mails
            # partent. La logique de retry reste gérée par la file pending_mails
            # (PendingMailProcessor). Décommenter pour repasser en asynchrone.
            # Symfony\Component\Mailer\Messenger\SendEmailMessage: async

            # Route your messages to the transports
            # 'App\Message\YourMessage': async
