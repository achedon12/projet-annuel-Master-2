# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

Two-app monorepo: a Symfony 7.1 JSON API in `backend/` and a Next.js 16 client in `frontend/`. Each has its own `CLAUDE.md` that loads automatically when you work inside that folder. The rules below are cross-cutting.

@.claude/rules/commands.md
@.claude/rules/auth-bridge.md
@.claude/rules/env.md
